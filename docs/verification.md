# 验收记录

日期：2026-09-08。本机独立 PostgreSQL 测试库，与交付预览数据库隔离。

## 自动验证

- Go 12 项测试通过，包含 `-race` 并发检查。
- 覆盖目标地址限制、302/禁止缓存、Cookie 复用与篡改、禁用 Cookie、范围去重、广告参数冲突、HEAD/机器人分类、登录和跨站请求防护、数据库写入失败继续跳转、花费覆盖日期、CSV 导入/导出、链接到期、地区查询及保留任务。
- `go vet ./...` 通过。
- 前端 5 项测试通过，含成本分母、查询参数、趋势补零及夏令时小时处理。
- 前端生产构建通过；图表分块有约 513 KB 的体积提示，已延迟加载。
- 前端依赖审计 0 漏洞。
- Go govulncheck：代码可触达漏洞 0；另提示依赖模块内有 1 项未被代码调用的问题。不是整个依赖树无任何通告的承诺。
- Go 工具链升级为 1.27.1，Gin 1.12.0、pgx 5.11.0，相关依赖已升级到修复版本。
- Docker 镜像构建通过，Compose 服务实际启动，数据库健康检查通过。
- DB-IP City Lite 官方 SHA1 校验通过；使用测试 IP 验证本地地区查询返回数据。

## 浏览器验证

- 登录、空数据总览、创建并绑定广告短链接、真实测试访问统计渲染已验证。
- 桌面及 390×844 视口布局检查通过，恢复桌面尺寸。
- 干净预览环境只保留默认链接，没有模拟业务数据。

## 最终容器版本压力测试

测试目标是本机 Go 容器 + PostgreSQL 容器，不跟随 302 到 WhatsApp。

- 发送速率：约 100 请求/秒，持续约 10 秒。
- 请求：1000；错误：0；数据库记录：1000。
- p50：2.98 ms；p95：6.65 ms；p99：11.36 ms。
- 单一测试来源触发频率规则：120 条 normal，880 条 suspicious，全部继续跳转。
- 压测链接、访问事件及相应审计记录已从交付预览库清理。

这是本机短时测试，不代表公网延迟、特定云服务器容量或长期稳定性保证。

## 备份恢复

使用 pg_dump custom 格式备份，恢复至独立空 PostgreSQL 数据库。验证默认链接 1 条、测试访问 0 条，恢复成功。

## 仍需上线环境验证

- 用户短域名、HTTPS 证书、代理真实 IP 与实际投放地区的 Cookie 策略。
- Facebook 广告账户的落地页检查。
- iPhone/Android 实机和 Facebook 内置浏览器的 WhatsApp 唤起，以及号码是否已注册。

当前完成本地可运行实现，没有对外发布，没有提交 GitHub。

## 2026-09-20：独立奇幻文学站

- 新增 `/novel/:code` 首页、文章列表和详情页；同一 code 复用现有目标地址、Meta 配置和归因信息。
- `click_events.surface` 区分 `short_link` 与 `novel`，两类签名票据不能交叉使用；小说站只允许手动咨询。
- 新增 8 篇原创英文演示内容和项目专用奇幻背景图，不使用参考站 Logo、正文或插画。
- 管理后台增加入口筛选、总览分项、访问明细标识和 CSV 字段。
- 小说前端 4 项测试、生产构建和 Brotli/gzip 预压缩通过；管理后台 26 项测试与格式检查通过；原产品落地页 24 项测试与构建通过。
- 后端 `go test ./...` 与 `go vet ./...` 通过。当前环境未配置专用 `TEST_DATABASE_URL` / `META_TEST_DATABASE_URL`，数据库集成用例按项目约定跳过。
- 浏览器实际检查桌面首页、列表、手机详情和沉浸模式；360、768、1440 三档页面宽度均无横向溢出。
- Docker 构建已启动，但拉取 `node:22-alpine` 基础镜像时长期无响应后人工终止；本地三个前端和 Go 构建验证不受影响，上线前需在网络正常环境重跑镜像构建。

## 2026-09-20：小说内容后台

- 小说改为 PostgreSQL 全局内容，后台支持新增、编辑、列表筛选、启停、唯一首页推荐和软删除；模型不包含作者字段。
- 正文使用 Markdown，后端统一移除原始 HTML、图片和危险链接后再输出；封面仅接受真实 JPEG、PNG、WebP，最大 5MB。
- 公开首页、列表和详情通过 `/novel-api/:code/*` 动态读取；短码只用于复用现有 WhatsApp、Meta 与统计配置，内容读取本身不写访问事件。
- Docker 增加 `novel_uploads` 持久卷，应用以非 root 用户写入 `/app/data/novel-uploads`。
- 后端全量测试、`go vet`、三个前端测试和生产构建、后台格式检查及差异检查通过；专用 PostgreSQL 测试库中的小说 CRUD、启停、推荐、软删除和公开 API 集成用例通过。
- Docker 镜像实际构建并更新原 `linkscope` 服务；原数据库内 8 条短链接保持，小说迁移写入 8 篇演示内容，健康检查正常。
- 浏览器实际检查后台小说列表、新增表单、Markdown 安全预览、公开桌面首页及 390px 详情；修正演示正文重复标题后复核只有一个作品标题，无横向溢出，沉浸模式可进入并退出。

## Element Plus 界面调整

- 改为浅色管理工作台，采用 Element Plus Container、Menu、Breadcrumb、Dropdown、DatePicker、Card、Statistic、Progress、Radio、Upload 等标准组件和官方图标。
- 新增链接名称／短码／广告 ID 搜索与状态筛选；日期支持今天、最近 7 天、最近 30 天快捷选择。
- 统一表格、弹窗、输入框、留白及空状态；广告归因表单分组。
- 前端 5 项测试及生产构建通过，浏览器核验导航、链接状态筛选、日期面板及页面布局。
- 无后端数据结构修改，未提交 GitHub。

## ngrok 公网配置

- 已授权将 sputter-untimely-pardon.ngrok-free.dev 从端口 8000 切换至 8080。
- PUBLIC_BASE_URL 改为公网 HTTPS 域名；已通过实际 TCP 连接确认可信代理为 172.20.0.1，并仅信任该地址。
- 管理员账号与密码按用户要求均为 admin。配置验证支持非空且最多 72 字节的密码，新增测试通过。
- 公网 API 验证通过：健康检查、admin 登录、Secure/HttpOnly/SameSite 会话属性、域名配置、两次 302 跳转、两条入库事件及地区识别。伪造 X-Forwarded-For 未覆盖实际访问地区。
- 测试链接及其访问记录已清理。COOKIE_MODE 仍为 off。
- 浏览器首次访问出现 ngrok 免费提示页；本应用无法替普通用户绕过它。
- ngrok 可重启配置及操作步骤保存于 deploy/ngrok.yml、NGROK.md；项目不保存 ngrok 登录 token。

## 2026-09-09：落地页与日志管理

- PostgreSQL 独立测试库完成全部后端集成测试，覆盖落地页 200、同内容展示、HTML 转义、凭证防篡改、过期/停用控制、按钮重复提交去重、访问群组统计和日志权限/脱敏。
- 修复公网浏览器表单 `Origin: null` 的误拦截，新增回归；签名凭证仍必需。落地页同源 Referrer 策略，外站不发送来源，CSP 允许 WhatsApp 官方跳转链。
- 前端 5 项工具测试通过，Docker 前端/后端构建通过。Vite 仍有现有 UI/图表包大于 500 KB 的体积提示。
- 实际浏览器验证：后台日志列表、登录日志详情脱敏、多肽页面桌面和 390px 手机布局。
- 公网 /123 打开返回落地页，点击按钮实际跳到 api.whatsapp.com 的指定号码页面；请求日志 POST /123/contact 为 303，访问记录具有按钮点击时间。没有发送 WhatsApp 消息。
- 本次浏览器验证产生少量真实请求/页面访问日志，保留用于排查，没有清除用户数据。修改前数据库备份位于忽略目录 backups/before-landing-logs-20260909.sql。

- 日志范围按最终要求调整为访客端：仅记录 /:code 与 /:code/contact；后台 API、健康检查和静态资源均排除。列表和详情查询仅返回访客日志；业务访问统计保持原口径。

- 定时跳转：全部后端测试通过，覆盖 0/1–300 秒校验、默认关闭、自动/手动分别记录、重复自动提交去重及已关闭定时的拒绝。前端测试与 Docker 构建通过。
- 在隔离测试库及本地 8081 服务中实测 5 秒倒计时，浏览器自动进入 api.whatsapp.com 指定号码页面；数据库确认 auto_redirected_at 有值、whatsapp_clicked_at 为空，日志 trigger=auto 且返回 303。正式 /123 保持 landing_delay=0。

## 2026-09-09：PEPLYRA 英文落地页

- 按参考官网更新英文首屏、六款产品卡片、本地图片及咨询区域；后台仍为中文。
- 独立 PostgreSQL 测试库的全部 Go 测试、前端 5 项测试及 Docker 生产构建通过。
- 浏览器验证英文标记、无中文页面文案、全部产品图片加载；390px 布局无横向溢出，咨询区域显示完整。
- 隔离预览点击英文咨询按钮后进入指定号码的 WhatsApp 官方页面，数据库记录手动咨询一次。未验证手机应用内聊天界面。
- 正式 /123 的现有 3 秒自动跳转设置及目标号码保持不变。

## 2026-09-09：静态资源性能优化

- 新增资源响应测试，验证 gzip 内容一致、编码协商、HEAD、无效路径、图片 304 和更换图片后的 ETag 失效。Go 非数据库测试与前端 5 项测试通过；本次未重跑数据库集成测试。
- 构建预生成 Brotli/gzip，首屏基础 JS/CSS 合计 1,024,724 字节降至 Brotli 224,930 字节（减少 78%），gzip 269,306 字节；两种格式均解压比对原始文件。
- Docker 构建与服务更新成功。公网实际响应验证 br、immutable；图片缓存校验成功。首页、认证 API、健康检查仍为 no-store。
- 浏览器验证后台统计卡片及趋势图正常渲染。ngrok 链路延迟不在本次修改范围内。

## 2026-09-09：咨询统计拆分与独立访客

- 总览保留网站访问、手动咨询点击、自动跳转和估算独立访客四张卡片，隐藏总入口请求、过滤后入口访问、可疑访问卡片。
- 趋势接口新增 auto_redirects，图表分别展示网站访问、手动咨询和自动跳转；明细可同时显示两类时间。
- 独立 PostgreSQL 测试库完整后端测试、前端 5 项测试、生产构建通过。新增回归验证重复自动提交去重后，手动和自动分别体现在汇总与趋势。
- 按用户授权将运行环境 COOKIE_MODE 改为 all；运行接口已确认生效。历史无访客标识记录不补算人数。
- 浏览器核对四张卡片正常显示；正式 b76ca014 汇总与趋势一致，历史网站访问 6、手动 1、自动 5、独立访客 0。

## 2026-09-09：可用短链接数量

- 新增 available_links 实时数据库统计：启用且未过期，独立于访问日期、广告及单链接筛选；无列表分页上限影响。
- 总览新增可用短链接总数卡片；咨询总数与手动、自动分项继续在同一卡片上下展示。
- 独立 PostgreSQL 全部后端测试、前端 5 项测试通过；覆盖无访问记录、停用/过期排除和筛选不改变可用总数。

## 2026-09-11：Meta 账户、Pixel、事件、来源与凭证升级

- 一个广告账户管理多个 Pixel，每条短链接选择一个目标；旧默认目标自动迁移。Insights 继续按账户同步和汇总。
- PageView 和手动咨询分别配置、按访问独立去重；目标和规则在访问时固化。自动跳转仍仅为内部统计。
- 凭证状态／到期日、加密密钥轮换、审计查询和来源诊断已接入后台。来源参数含 Token 或未展开宏时提示问题；不访问粘贴的网址。
- 最终完整 Go `go test -race ./... -count=1` 和 `go vet ./...` 通过，分别使用隔离的应用与 Meta 数据库。后台前端 30 项测试、落地页 9 项测试、两个生产构建和后台格式检查通过。
- 回归验证：跨账户目标拒绝、规则快照、浏览／手动重复提交、过期读取凭证禁止排队／调度／发送、默认凭证清除保护、审计真实列名、动态来源优先级、敏感参数过滤，以及 `k1x` 切换 `k_x` 后删除旧密钥仍能解密凭证和待发事件。
- 浏览器在隔离环境验证账户／Pixel 到期日保存、链接选择 Pixel、凭证及审计显示、来源生成与诊断。一次落地页打开产生一条入口访问和一条 204 浏览回调，浏览回调不重复计网站访问，账户关闭时未产生 Meta 事件。
- 数据迁移先在备份副本上预演，再更新现有 8080 服务。8 条链接、238 条访问、2 个账户、1 条回传记录保留；生成 2 个兼容默认 Pixel，凭证保持一致。备份为 `backups/db-before-meta-upgrade-20260911.dump`，旧镜像为 `linkscope-app:before-meta-upgrade-20260911`。
- 运行后本地健康检查、Meta 账户／Pixel／凭证／审计／事件接口均返回 200；浏览器确认默认 Pixel 和旧事件正常展示。独立代码复核通过，未提交 Git。
- 本次使用模拟 Graph 接口验证发送行为，没有向真实 Meta 发送新增测试或正式事件。真实广告读取授权、测试事件页面与归因结果需按账户实际情况核对。
- 公网检查发现现有 ngrok 域名转发至 8000 的其他服务，当前项目为 8080；未修改共享隧道，已向用户确认是否切回。本地实现和服务更新已完成。

## 2026-09-21：整体更名为语音小说

- 独立前端工程、Gin 模块、后台菜单和当前文档统一改为“语音小说”；公开入口为 `/audio-novel/:code`，后台入口为 `/admin/audio-novels`，内容接口为 `/audio-novel-api/:code/*`。
- PostgreSQL 使用原地迁移把 `novels` 改为 `audio_novels`，把历史访问类型从 `novel` 改为 `audio_novel`；上线数据核对为 9 条内容、ID 1–9 保持不变、4 条历史访问已转换，旧表和旧访问类型均不存在。
- 历史封面路径通过迁移 017 从 `/novel-uploads/*` 转为 `/audio-novel-uploads/*`。旧卷只读复制到 `linkscope_audio_novel_uploads` 后，两卷均为 4 个文件且逐文件一致；旧卷保留用于回滚。
- Docker 已重新构建并启动同一个 Gin 服务，健康检查、前台首页／列表／详情、公开内容接口、后台入口和历史封面均返回 200；旧公开、资源、上传及后台 API 路径均返回 404。
- PageView、WhatsApp 咨询和 TimeSpent 仍使用同一短码配置；无效签名请求均返回 400。为避免产生真实 Meta 或 WhatsApp 行为，本次运行验收未提交有效事件，完整有效流程由隔离数据库集成测试覆盖。
- 浏览器实际检查桌面首页、列表、详情、两页分页、字号与专注模式；390×844 窄屏检查菜单展开、跳转和布局。可见停留计时从首页跨列表、详情持续累加，未发现横向溢出、裁切、旧“小说管理”标签或损坏资源。
- 后台实际检查语音小说列表、新增和编辑表单，确认 Markdown 正文、封面上传、启停、推荐及删除控件存在；验收过程中未保存、停用、推荐或删除任何现有数据。
- Go 全量测试与静态检查、三个 Vue 应用的测试和生产构建、后台格式检查、运行契约脚本及差异检查全部通过。Vite 仅保留原有的大分块体积提示，无新增运行错误。
- 本次仅更新本地工作树和本机容器，没有提交或推送 GitHub。
